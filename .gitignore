# dependências
/node_modules

# builds do Next / exportação estática
/.next
/out
next-env.d.ts

# fontes originais pesadas (fotos em alta e vídeos) — manter backup local, fora do deploy
/Prints-referencias-instagram
# Arte de origem da fadinha: 5 MB de PNG que viram WebP em public/images/marca/fadinha/
/Fadinha

# ambiente / segredos
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*

# sistema
.DS_Store
*.pem
npm-debug.log*

# gerado por `wrangler types` a partir do wrangler.jsonc
worker-configuration.d.ts

# cache incremental do tsc
tsconfig.tsbuildinfo

# estado local do wrangler — inclui o SQLite do D1 de desenvolvimento.
# Testar o formulário localmente grava leads de verdade neste arquivo, e o
# repositório é público: nome, telefone e dados de criança não podem entrar aqui.
.wrangler/
